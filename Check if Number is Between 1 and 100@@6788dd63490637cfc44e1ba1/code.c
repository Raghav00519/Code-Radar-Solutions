#include <stdio.h>

int main() {
    int num;

        // Input a single integer
            scanf("%d", &num);

                // Check if the number is in the range 1 to 100
                    if (num >= 1 && num <= 100) {
                            printf("In Range\n");
                                } else {
                                        printf("Out of Range\n");
                                            }

                                                return 0;
                                                }
                                                








