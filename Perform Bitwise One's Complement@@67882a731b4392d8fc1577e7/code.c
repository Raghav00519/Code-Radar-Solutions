#include <stdio.h>

int main() {
    int number, result;
        
            // Reading a single integer
                scanf("%d", &number);
                    
                        // Perform the bitwise one's complement operation
                            result = ~number;
                                
                                    // Print the result
                                        printf("%d\n", result);
                                            
                                                return 0;
                                                }
                                                












